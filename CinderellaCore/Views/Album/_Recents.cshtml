@using CinderellaCore.Model.Models
@model List<CinderellaCore.Model.Models.Album>

<div class="container">
    @foreach (var album in Model)
    {
        @await Html.PartialAsync("~/Views/Album/_Album.cshtml", album)
    }
</div>