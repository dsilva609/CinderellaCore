@using CinderellaCore.Model.Models
@model List<CinderellaCore.Model.Models.Game>

<div class="container">
    @foreach (var game in Model)
    {
        @await Html.PartialAsync("~/Views/Game/_Game.cshtml", game)
    }
</div>